<%= simple_form_for(@article) do |f| %>
  <%= f.error_notification %>

  <!-- div class="form-inputs" -->
    <%= f.input :title, label: "Name" %>
    <%= f.input :body, label: "Notes" %>
  <!-- /div -->

  <!-- div class="form-actions" -->
    <%= f.button :submit, "Create Tone Bank" %>
  <!-- /div -->
<% end %>
